// Importing source headline tables into this Convex project is no longer supported.
// The llm-service reads from the original Convex deployment and writes only
// llm-specific data into its own target deployment.
export {};
